import { IContainsPrereleaseCommitOptions } from './i-contains-prerelease-commit-options';

export interface IContainsReleaseCommitOptions {
  newCommits: IContainsPrereleaseCommitOptions['newCommits'];
}
